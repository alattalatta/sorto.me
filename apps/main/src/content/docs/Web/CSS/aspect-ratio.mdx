---
title: 'CSS aspect-ratio: 가로세로비'
bcd: css.properties.aspect-ratio
---

CSS **`aspect-ratio`** 속성은 요소 박스의 *선호 가로세로비*를 설정합니다. 선호 가로세로비는 자동 크기와 일부 레이아웃 내에서의 크기 계산에 사용됩니다.

```css hidden name=demo
img {
  aspect-ratio: auto;
  /* aspect-ratio: 1 / 1; */
  /* aspect-ratio: 16 / 9; */
  /* aspect-ratio: 2; */
}
```

```css hidden editable=false name=demo filename=style
img {
  width: auto;
  height: 200px;
}
```

```html hidden name=demo
<img src="/assets/lion.jpg">
```

<LiveExample height={240} lang="css" name="demo" />

# 구문

```css
aspect-ratio: 1 / 1;
aspect-ratio: 1;

/* 전역 값 */
aspect-ratio: inherit;
aspect-ratio: initial;
aspect-ratio: revert;
aspect-ratio: revert-layer;
aspect-ratio: unset;
```

## 값

- : `auto`
  - 내재된 가로세로비를 가지는 [대체 요소](/docs/Web/CSS/Replaced_element)는 그 비율을 사용하고, 이외에는 선호 가로세로비를 설정하지 않습니다. 내재된 가로세로비를 사용한 크기 계산에는 항상 콘텐츠 박스의 크기를 사용합니다.
- : [`<ratio>`](https://developer.mozilla.org/en-US/docs/Web/CSS/ratio)
  - 요소 박스의 선호 가로세로비를 `width` / `height`의 비율로 설정합니다. `height`는 `1`의 기본 값을 가지고 있으며 슬래시와 함께 생략할 수 있습니다. 선호 가로세로비를 사용한 크기 계산에는 [`box-sizing` 속성](https://developer.mozilla.org/en-US/docs/Web/CSS/box-sizing)으로 지정한 박스의 크기를 사용합니다.

# 명세

[CSS Box Sizing Module Level 4](https://drafts.csswg.org/css-sizing-4/#aspect-ratio)

# 브라우저 호환성

<BrowserCompat data={props.bcd} />
