---
title: 'HTML <span> 요소'
description: HTML <span> 요소는 구문 콘텐츠를 위한 범용 인라인 컨테이너입니다.
bcd: html.elements.span
---

**HTML `<span>` 요소**는 구문 콘텐츠를 위한 범용 인라인 컨테이너입니다.

```html hidden name=demo
<article>
  <p>
    <span class="ingredient">식용유</span>를 팬에 넣고 가열해주세요. 아지랑이가 피어오르기 시작하면 준비해뒀던
    <span class="ingredient">안심</span>을 올리고, 30초마다 뒤집어주세요.
  </p>
  <article></article>
</article>
```

```css hidden name=demo
.ingredient {
  color: red;
}
```

<LiveExample height={180} lang="html" name="demo" />

`<span>`은 "순수" 컨테이너 요소이며 본질적인 의미를 가지지 않습니다. 대신 다른 요소 여럿을 묶어서 `class`나 `id` 특성을 적용하거나, `lang` 특성을 사용해 문서의 특정 구역이 다른 언어임을 표시하는 등의 용도로 사용할 수 있습니다.

# 특성

[전역 특성](/docs/Web/HTML/Global_attributes)만 포함합니다.

# 명세

[HTML Standard](https://html.spec.whatwg.org/multipage/text-level-semantics.html#the-span-element)

# 브라우저 호환성

<BrowserCompat data={props.bcd} />

# 같이 보기

- 블록 범용 컨테이너 [`<div>`](./div)
