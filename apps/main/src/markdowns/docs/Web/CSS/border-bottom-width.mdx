---
title: 'CSS border-bottom-width: 아래 테두리 두께'
titleShort: 'border-bottom-width'
bcd: css.properties.border-bottom-width
---

CSS **`border-bottom-width`** 속성은 요소의 아래 테두리 두께를 설정합니다.

```css hidden name=demo filename=style
#target {
  background: lightblue;
  border: 1px dashed #0006;
  border-bottom-color: red;
  border-bottom-style: solid;
  height: 100px;
  width: 200px;
}
```

```css hidden name=demo
#target {
  border-bottom-width: 4px;
  /* border-bottom-width: 0.5em;   */
  /* border-bottom-width: thick; */
  /* border-bottom-width: 0; */
}
```

```html hidden name=demo
<div id="target"></div>
```

<LiveExample height={180} lang="css" name="demo" />

# 구문

```css-prop
/* <length> 값 */
border-bottom-width: 10em;
border-bottom-width: 3vmax;
border-bottom-width: 6px;

/* 키워드 값 */
border-bottom-width: thin;
border-bottom-width: medium;
border-bottom-width: thick;

/* 전역 값 */
border-bottom-width: inherit;
border-bottom-width: initial;
border-bottom-width: revert;
border-bottom-width: revert-layer;
border-bottom-width: unset;
```

`border-bottom-width` 속성에는 한 개의 키워드 또는 [`<length>`] 값을 사용합니다.

## 값

- : `<line-width>`
  - 0 이상의 [`<length>`], 또는 테두리의 두께를 나타내는 키워드 값 중 하나입니다. 키워드 값은 다음 중 하나여야 합니다.
    - `thin`
    - `medium`
    - `thick`

    > [note] CSS 명세가 키워드에 대응하는 두께를 정의하진 않았으므로 구현체마다 정확한 값은 다를 수 있습니다. 그러나 항상 `thin ≤ medium ≤ thick` 순서로 두께가 증가하며, 단일 문서 내에서는 값이 일정합니다.

# 예제

## 키워드 두께 비교하기

```css hidden name=keywords
.thin {
  border-bottom-width: thin;
}
.medium {
  border-bottom-width: medium;
}
.thick {
  border-bottom-width: thick;
}
```

```css hidden name=keywords filename=style
.thin, .medium, .thick {
  border-bottom-color: red;
  border-bottom-style: solid;
  height: 30px;
  margin-bottom: 8px;
  text-align: center;
  width: 200px;
}
```

```html hidden name=keywords
<div class="thin">thin</div>
<div class="medium">medium</div>
<div class="thick">thick</div>
```

<LiveExample height={200} lang="css" name="keywords" />

# 명세

- [CSS Backgrounds and Borders Module Level 3](https://drafts.csswg.org/css-backgrounds/#the-border-width)

# 브라우저 호환성

<BrowserCompat data={props.bcd} />

# 같이 보기

- 다른 방향: [`border-top-width`], [`border-right-width`], [`border-left-width`]
- 네 방향 단축 속성: [`border-width`]

[`<length>`]: ./length
[`border-left-width`]: ./border-left-width
[`border-right-width`]: ./border-right-width
[`border-top-width`]: ./border-top-width
[`border-width`]: ./border-width
