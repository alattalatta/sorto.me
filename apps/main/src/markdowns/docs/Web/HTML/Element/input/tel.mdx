---
title: 'HTML <input type="tel">: 전화번호 입력'
titleShort: '<input type="tel">'
bcd: html.elements.input.type_tel
---

**`tel` 유형의 [`<input>`](../input) 요소**는 사용자가 전화번호를 입력할 수 있는 텍스트 입력 칸을 제공합니다. [`email`](./email)이나 [`url` 유형](./url)과는 달리 `tel`은 입력 형식 검증을 내장하지 않는데, 국가와 지역마다 전화번호의 형식이 너무 다양하기 때문입니다.

```html hidden name=demo
<label for="phone">
  전화번호<br>
  <small>형식: 010-0000-0000</small>
</label>
<input id="phone" name="phone" pattern="010-[0-9]{4}-[0-9]{4}" type="tel" />
```

```css hidden name=demo
label {
  display: block;
}
```

<LiveExample height={180} lang="html" name="demo" />

`tel` 유형은 형식 검증을 내장하지 않는 등 `text` 유형과 사실상 동일한 입력 칸을 제공하지만, 그럼에도 유용한 부분이 있습니다. 가장 큰 장점은 모바일 기기의 브라우저가 일반 가상 키보드 대신 전화번호 입력에 최적화된 키패드를 표시한다는 점입니다.

# 값

`tel` 유형의 값은 문자열입니다. 값의 형식을 제한하려면 [`minlength`](#term-minlength), [`maxlength`](#term-maxlength), [`pattern` 특성](#term-pattern) 등을 사용하세요.

# 추가 특성

`tel` 유형은 모든 [`<input>`](../input) 요소가 공유하는 특성 외에도 아래의 특성을 추가로 지원합니다.

- : `list`
  - 현재 문서 내에 존재하는 [`<datalist>` 요소][`<datalist>`]의 [`id` 특성][`id`]을 지정하면 사전 설정된 추천 입력값을 보여줄 수 있습니다. `<datalist>`에 정의됐지만 현재 `<input>` 유형과 호환되지 않는 값은 추천 목록에 포함되지 않습니다. 추천 목록은 추천 사항일 뿐이며 필수 사항이 아니므로 사용자는 목록을 무시하고 직접 값을 입력할 수 있습니다.

- : `maxlength`
  - 입력 값의 유효한 최대 문자 수(UTF-16 코드 유닛 기준)를 지정합니다. 0 이상의 정수만 지정할 수 있습니다. 유효하지 않은 값은 지정해도 최대 길이를 설정하지 않습니다.

    이 값보다 긴 길이의 텍스트를 받은 경우 `<input>`이 [제약 검증](https://developer.mozilla.org/en-US/docs/Web/Guide/HTML/Constraint_validation)을 통과하지 못합니다. 기본 설정에서, 브라우저는 사용자가 입력하는 값의 길이를 이 특성의 값으로 제한합니다.

- : `minlength`
  - 입력 값의 유효한 최소 문자 수(UTF-16 코드 유닛 기준)를 지정합니다. 0 이상의 정수만 지정할 수 있습니다. 유효하지 않은 값은 지정해도 최대 길이를 설정하지 않습니다.

    이 값보다 짧은 길이의 텍스트를 받은 경우 `<input>`이 [제약 검증](https://developer.mozilla.org/en-US/docs/Web/Guide/HTML/Constraint_validation)을 통과하지 못합니다.

- : `pattern`
  - [제약 검증](https://developer.mozilla.org/en-US/docs/Web/Guide/HTML/Constraint_validation)을 만족하려면 입력 값이 일치해야 하는 정규표현식을 지정합니다. JavaScript [`RegExp`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp)가 인식할 수 있는 정규표현식이어야 합니다. 정규표현식을 컴파일할 때 `u` 플래그를 지정하므로 패턴은 항상 Unicode 코드 포인트의 시퀀스로 취급합니다. 정규표현식을 열고 닫는 슬래시는 생략해야 합니다.

    유효하지 않은 값을 지정한 경우 무시합니다.

    > [note] `pattern` 특성을 사용할 땐 가까운 곳에 기대 형식에 대한 설명 텍스트를 배치하세요. 또한 [`title` 특성](../../Global_attributes#term-title)으로 추가 도움말을 제공할 수 있습니다. 대부분의 브라우저에서는 `title`의 내용을 툴팁으로 표시합니다.
    >
    > 접근성에서, 설명 텍스트는 필수 사항이고 툴팁은 보조 기능입니다.

    아래의 [입력 패턴](#입력-패턴)에서 추가 설명과 예제를 확인하세요.

- : `placeholder`
  - 사용자가 입력해야 하는 값을 간략하게 알려줄 텍스트를 지정합니다. 값이 개행 문자를 포함해서는 안됩니다. `placeholder`는 이 입력 칸의 설명을 제공하는 것이 아니라, 기대하는 데이터의 유형을 나타내야 합니다.

    > [note] `placeholder`는 양식을 설명할 수 있는 다른 방법에 비해 적합하지 않으며 예상하지 못한 기술적 문제를 유발할 수 있습니다. [`<input>`의 레이블 절](../input#플레이스홀더는-접근성이-떨어짐)을 참고하세요.

- : `readonly`
  - 입력 칸을 읽기 전용으로 만들어 사용자가 편집할 수 없도록 하는 불리언 특성입니다. 하지만 JavaScript를 사용해 [HTMLInputElement](https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement)의 `value` 속성을 직접 설정하면 값을 바꿀 수 있습니다.

    > [note] 읽기 전용 입력 칸에는 사용자가 값을 입력할 수 없으므로, `readonly`를 추가한 요소에 [`required` 특성](../input#term-required)을 추가하면 아무 효과도 없습니다.

# 유효성 검증

`tel` 입력 칸은 `text` 유형과 마찬가지로 기본 유효성 검증을 포함하지 않습니다. [`pattern`](#term-pattern)을 사용하면 전화번호 입력 형식을 강제할 수 있습니다.

> [note] HTML 양식 검증은 서버에서 입력 데이터를 검증하는 것의 대안이 될 수 없습니다. HTML 코드를 직접 편집해서 검증을 우회하거나 아예 삭제하는 게 너무나 쉽기 때문입니다. 게다가 HTML을 아예 거치지 않고 서버로 직접 요청을 전송해버릴 수도 있습니다. 서버 측 코드가 입력 데이터 검증에 실패한다면 잘못된 형식이나 유형이나 크기의 데이터가 데이터베이스에 저장되어 큰 문제가 발생할 수 있습니다.

## 유효성 스타일

[`:valid`]와 [`:invalid` 의사 클래스][`:invalid`]를 활용하면 사용자에게 입력 값이 유효한지 시각적으로 알려줄 때 유용합니다. 아래 예제들에서는 다음과 같은 스타일을 적용해서 유효한 값을 받으면 초록색 체크 표시를, 유효하지 않은 값을 받으면 빨간색 X 표시를 보여주겠습니다.

```css
input:invalid + span::after {
  color: red;
  content: '✖';
  padding-left: 5px;
  position: absolute;
}

input:valid + span::after {
  color: green;
  content: '✓';
  padding-left: 5px;
  position: absolute;
}
```

위 스타일을 적용하면, `<input>` 요소 바로 다음에 배치된 `<span>` 요소에 체크나 X 표시를 합니다.

## 필수 값

[`required` 특성](#term-required)을 지정하면 필수 입력 칸을 쉽게 만들 수 있습니다. 사용자는 반드시 이 칸을 채워야 양식을 제출할 수 있습니다.

```html hidden name=required
<form>
  <div>
    <label for="phone">전화번호:</label>
    <input id="phone" name="phone" required type="tel" />
    <span class="validity"></span>
  </div>
  <button>제출</button>
</form>
```

```css hidden editable=false name=required
button {
  margin-top: 10px;
}

input:invalid + span::after {
  color: red;
  content: '✖';
  padding-left: 5px;
  position: absolute;
}

input:valid + span::after {
  color: green;
  content: '✓';
  padding-left: 5px;
  position: absolute;
}
```

<LiveExample height={200} lang="html" name="required" />

## 입력 패턴

[`pattern` 특성](#term-pattern)을 사용하면 입력 값이 반드시 따라야 할 정규표현식을 지정할 수 있습니다([정규표현식으로 검증하기](https://developer.mozilla.org/en-US/docs/Learn_web_development/Extensions/Forms/Form_validation#validating_against_a_regular_expression)를 참고하세요). 사용자는 반드시 이 정규표현식과 일치하는 값을 입력해야 양식을 제출할 수 있습니다.

아래 예제에서는 `010-****-****` 형식의 전화번호를 받아야 제출할 수 있습니다.

```html hidden name=pattern
<form>
  <div>
    <label for="phone">전화번호:</label>
    <input id="phone" name="phone" pattern="010-[0-9]{4}-[0-9]{4}" required type="tel" />
    <span class="validity"></span>
    <p>010-1234-5678처럼 입력하세요.</p>
  </div>
  <button>제출</button>
</form>
```

```css hidden editable=false name=pattern
button {
  margin-top: 10px;
}

p {
  color: gray;
  font-size: 0.8em;
  margin-bottom: 0;
}

input:invalid + span::after {
  color: red;
  content: '✖';
  padding-left: 5px;
  position: absolute;
}

input:valid + span::after {
  color: green;
  content: '✓';
  padding-left: 5px;
  position: absolute;
}
```

<LiveExample height={200} lang="html" name="pattern" />

하이픈 (`-`) 없이 숫자만 입력받도록 하려면 아래와 같이 작성할 수 있습니다.

```html hidden name=pattern2
<form>
  <div>
    <label for="phone">전화번호:</label>
    <input id="phone" name="phone" pattern="010[0-9]{8}" required type="tel" />
    <span class="validity"></span>
    <p>하이픈 없이 숫자만 입력하세요.</p>
  </div>
  <button>제출</button>
</form>
```

```css hidden editable=false name=pattern2
button {
  margin-top: 10px;
}

p {
  color: gray;
  font-size: 0.8em;
  margin-bottom: 0;
}

input:invalid + span::after {
  color: red;
  content: '✖';
  padding-left: 5px;
  position: absolute;
}

input:valid + span::after {
  color: green;
  content: '✓';
  padding-left: 5px;
  position: absolute;
}
```

<LiveExample height={200} lang="html" name="pattern2" />

# 명세

- [HTML Standard](<https://html.spec.whatwg.org/multipage/input.html#telephone-state-(type=tel)>)

# 브라우저 호환성

<BrowserCompat data={props.bcd} />

# 같이 보기

- 이메일 입력 칸: [`<input type="email">`](./email)
- 숫자 입력 칸: [`<input type="number">`](./number)
- 일반 텍스트 입력 칸: [`<input type="text">`](./text)

[`<datalist>`]: ../datalist
[`:invalid`]: https://developer.mozilla.org/en-US/docs/Web/CSS/:invalid
[`:valid`]: https://developer.mozilla.org/en-US/docs/Web/CSS/:valid
[`id`]: /docs/Web/HTML/Global_attributes/id
