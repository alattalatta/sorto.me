---
title: HTML <figure> 요소
excerpt: HTML <figure> 요소는 독립적인 콘텐츠를 나타내며, 선택적으로 <figcaption>을 사용해 설명을 제공할 수 있습니다.
---

**HTML `<figure>` 요소**는 독립적인 콘텐츠를 나타내며, 선택적으로 [`<figcaption>`](./figcaption)을 사용해 설명을 제공할 수 있습니다.

```html
<figure>
  <img src="/media/example/elephant.jpg" alt="진흙 목욕을 하는 코끼리">
  <figcaption>코끼리의 생태</figcaption>
</figure>
```

# 특성

[전역 특성](/docs/Web/HTML/Global_attributes)만 포함합니다.

# 사용 일람

- `<figure>`는 이미지, 삽화, 도표, 코드 조각 처럼, 문서의 주요 맥락에서 참조하지만 다른 위치나 부록으로 이동해도 영향을 주지 않는 부분을 표현합니다.
- [`<figcaption>`](./figcaption) 요소를 `<figure>`의 처음 또는 마지막 자식으로 포함해서 설명을 나타낼 수 있습니다. 복수의 `<figcaption>`이 존재하는 경우 가장 처음의 `<figcaption>`만 `<figure>`의 설명으로 취급합니다.

# 명세

[HTML Living Standard](https://html.spec.whatwg.org/multipage/semantics.html#the-figure-element)

# 브라우저 호환성

<BrowserCompat>html.elements.figure</BrowserCompat>
