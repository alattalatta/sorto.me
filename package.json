{
  "name": "sorto.me",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build --scope='@app/web' --no-deps --include-dependencies",
    "prebuild": "turbo run prebuild --scope='@app/web' --no-deps --include-dependencies",
    "dev": "turbo run dev",
    "turbo": "turbo"
  },
  "workspaces": [
    "apps/**",
    "packages/**"
  ],
  "devDependencies": {
    "@alattalatta/eslint-config": "^0.5.0",
    "@alattalatta/prettier-config": "^0.2.0",
    "eslint": "^8.11.0",
    "eslint-config-next": "^12.1.0",
    "prettier": "^2.5.1",
    "turbo": "^1.1.6",
    "typescript": "^4.6.2"
  },
  "packageManager": "pnpm@6.32.3",
  "prettier": "@alattalatta/prettier-config"
}
