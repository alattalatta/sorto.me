import { vars } from '@lib/ui/theme.css'
import { style } from '@vanilla-extract/css'

export const caption = style({
  color: vars.colors.fg2,
  fontSize: 12,
  marginTop: 16,
  textAlign: 'right',
})
